name: Deploy to Dothome

on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Get latest code
        uses: actions/checkout@v4

      - name: Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }} # 예: changdae.dothome.co.kr
          username: ${{ secrets.FTP_USERNAME }} # 닷홈 아이디
          password: ${{ secrets.FTP_PASSWORD }} # FTP 비밀번호
          local-dir: ./html/ # 로컬 업로드 대상 폴더
          server-dir: /html/ # 서버 저장 경로 (닷홈은 보통 /html/)
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/*.md
